这是一个运行于create react app的pytorch可视化构建器，可以通过想搭积木一样完成module的搭建，简单的配置输入参数，同时也可以下载已有的模型并将其拆解为多层的神经网络，进行可视化的ui修改。

通过运行:

```bash
npm install

npm start
```

会自动打开`http://localhost:3000`，项目就可以成功运行了。

![show](img/show.png)
